export class City{
    id:number;

    name:string;

    postalCode:string;

    constructor(id:number,name:string,postalCode:string){
        this.id=id;
        this.name=name;
        this.postalCode=postalCode;
    }
}